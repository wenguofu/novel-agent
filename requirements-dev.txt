# novel-agent dev/test dependencies (M3.1)
# Install with: pip install -r requirements-dev.txt

# Test framework + coverage
pytest>=7.4
pytest-cov>=4.1
pyyaml>=6.0       # For yaml outline parsing (e.g., portal/app.py, outline sync)
