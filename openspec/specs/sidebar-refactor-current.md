# 侧边栏重构 — 当前状态

## 现状
- 侧边栏所有页面平铺，共 24 个链接
- 所有页面无需选择小说即可进入
- 每个小说专属页面内部有自己的小说选择下拉框
- 页面间切换不保留小说状态

## 问题
- 侧边栏过长，难以导航
- 人物管理、伏笔管理等属于特定小说，不应全局可见
- 每次进入页面都要重新选小说
- 没有"当前小说"上下文

## 代码现状
- `index.html`: 侧边栏 `<nav>` 内 24 个 `<a>` 标签，无小说选择器
- `app.js`: `App.currentNovel = null` 已定义但未使用
- 每个小说专属页面自己 `getElementById('xxNovel').value` 读取小说
