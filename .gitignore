# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~

# Temp
tmp/
temp/
*.log

# Python
venv/
__pycache__/
*.pyc
*.pyo

# IDE
.codebuddy/
.trae/
.idea/
.vscode/

# AI tooling (local config, not part of repo)
.claude/    # Claude Code
.codex/     # Codex CLI
.cursor/    # Cursor IDE
.aider*     # Aider
.hermes/    # 旧调度器
.superpowers/  # 已有规则

# Agent system artifacts (not part of the novel)
agent-system/workflows/*.md

# Novels (content only, not code)
novels/

# RAG database
# (portal/*.db was the v3 SQLite path; the portal is MySQL-only since
# v3.4. Snapshots of the last pre-migration SQLite files live under
# migration_backup/sqlite_snapshot_*/.)
chroma_db/
*.db
*.db-shm
*.db-wal
*.db.bak
*.db.testhidden

# Portal runtime artifacts
portal/.encryption_key
portal/.coverage

# M3: agent code review reports
.code-reviews/

# Tooling caches (local to each machine)
.codegraph/
.understand-anything/
.understandignore
.coverage
.coverage.*
