# novel-agent 依赖 (v3.1 harness-optimized)
# pip install -r requirements.txt

# RAG 向量检索
chromadb>=1.5.0
sentence-transformers>=3.0.0
PySocks>=1.7.0

# API Key 加密
cryptography>=41.0.0

# Web Portal
flask>=3.0
flask-cors>=4.0
httpx>=0.25

# v3.1: Prompt template engine
jinja2>=3.0
pydantic>=2.0

# v3.1: Database abstraction (SQLite default, MySQL optional)
sqlalchemy>=2.0
# pymysql>=1.1  # Uncomment for MySQL support

# v3.1: API resilience
tenacity>=9.0

# Agent 执行框架
jsonschema>=4.20

# 合规检查
# (stdlib only — 无额外依赖)

# 节奏检查
# (stdlib only — 无额外依赖)

# M3: pre-commit + coverage
pre-commit>=3.5
pytest-cov>=4.1
